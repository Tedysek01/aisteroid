import { promptsMetadata } from '../metadata';

export const metadata = promptsMetadata;

export default function PromptsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
} 